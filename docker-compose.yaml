
services:
  cmdstanpy:
    build:
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    container_name: collapse
    ports:
      - "8008:8888"
      - "9009:9999"
    command: jupyter lab --ip=0.0.0.0 --no-browser --allow-root 
